{
  "name": "rc3-browser-worker",
  "version": "0.1.0",
  "private": true,
  "description": "RC3.2 Browser Worker — entrenament distribuït LoRA (node de càlcul)",
  "scripts": {
    "build": "vite build",
    "build:tsc": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:playwright": "playwright test",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^26.1.1",
    "playwright": "^1.45.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
