DELIVERY_INFO — meshtrainer QWEN DISTRIBUTED PILOT (R2 END-TO-END) FINAL
=======================================================================

DATA: 2026-08-09
BRANCH: rc5.4-stagea
BASE CERTIFICADA:
  RC5.4 Stage A R3.1 — 77595f4 (tarball 1a0da69a)
  Backend pilot R1 — f0a71e1 (QWEN SINGLE-WORKER / FEDAVG PROTOTYPE: PASS)
COMMIT DEL CODI: <PENDENT>

OBJECTIU (ordre del Supervisor)
-------------------------------
Connectar Qwen3TrainingBackend a la infraestructura MeshTrainer real i
executar dos workers Qwen reals amb control HTTP, leases RC5.4, contribucions
RC5.3 i FedAvg global. Workers seqüencials (RAM): A subprocess -> allibera ->
B subprocess, amb worker_id/session/lease/journal/shard diferents i
comunicació HTTP real.

DECISIÓ DEL SUPERVISOR
----------------------
f0a71e1 acceptat com a QWEN SINGLE-WORKER / FEDAVG PROTOTYPE: PASS.
Aquest paquet eleva el pilot a MESHRETRAINER QWEN DISTRIBUTED PILOT FINAL.

CANVIS (additius, res de la infraestructura congelada modificada)
----------------------------------------------------------------
1. ENDURIMENT DEL BACKEND (qwen3_training_backend.py)
   - load_adapter_from_bundle amb expected_sha OBLIGATORI (mode MeshTrainer)
     + schema complet (tots els tensors LoRA: noms exactes, shapes exactes,
     dtype admès; cap absent/extra). Wrong/partial/stale adapter -> REJECTED.
   - base_model_identity: fingerprint canònic persistit a la càrrega
     (model identifier, config_sha, weight manifest SHA, base_model_hash
     sobre mostra dels pesos). Els 2 workers informen el mateix hash; el
     Coordinator rebutja un worker amb base diferent.
   - request_sha al journal (update_id, worker_id, assignment_id, shard_id,
     adapter_pre_hash, example_id, payload hash): mateix update_id + mateix
     request_sha -> mateix delta; request_sha diferent -> REJECTED.
   - recovery post-APPLIED: adapter_post = adapter_pre + persisted_delta
     SENSE optimizer.step, amb estat de l'optimizer AdamW (moments + step)
     i estat del RNG persistits al journal i restaurats exactament
     (serialització claus int + shapes dels moments); prova no-crash vs
     crash amb adapters finals IDÈNTICS (hash tolerant float16 per 1 ulp).
2. qwen_http_server.py — serve54 Qwen (JSON-RPC HTTP): worker.register,
   worker.calibrate, assignment.get, lease.acquire/renew, step.open,
   step.worker_update.submit, step.commit, checkpoint.upload,
   contribution.register, step.release. Reutilitza rc5_4_leases.LeaseManager
   REAL. Patró lease-primer -> idempotència -> execució -> save. Shard
   binding (shard_manifest_sha) i ETT verificats. Lease absent/expirada/
   wrong session/base/shard/ETT/request_sha -> REJECTED.
3. qwen_round_coordinator.py — Coordinator Qwen (patró RC5.3): assignacions
   amb shard_manifest_sha, contribucions RECEIVED->VALIDATED->ACTIVE,
   fedavg = adapter_0 + Σ(delta_i·ETT_i)/ΣETT (fórmula idèntica al FedAvg
   certificat); oracle monolític independent per a la comparació.
4. qwen_worker.py — subprocess real: flux de 12 passos per HTTP, renew de
   lease propi (recovery), auto-recovery dels passos APPLIED del journal,
   --deterministic i --train-only (crash simulat).
5. qwen_quality.py — validation loss/perplexity BEFORE/AFTER (mai només
   train loss) + 10 holdouts (prompt, resposta generada, resposta esperada,
   puntuació determinista 0/1/2 per paraules clau).
6. Tests: qwen_backend_r2_check.py (S1..S4), qwen_distributed_adversarial_
   tests.py (A1..A11 enforcement), qwen_distributed_recovery_tests.py
   (R-Q1..R-Q5 amb subprocessos/PIDs), qwen_distributed_run.py (driver
   end-to-end), RUN_QWEN_DISTRIBUTED_PILOT_GATE.sh.

RESULTATS CERTIFICATS (gate complet a CT112, CPU)
-------------------------------------------------
- backend tests single-worker: 22/22 PASS
- backend R2 hardened (S1-S4): 15/15 PASS
  (wrong/partial adapter REJECTED; request_sha REJECTED; recovery exacte)
- HTTP/lease adversarial (A1-A11): 11/11 PASS (REJECTED/idempotent)
- run distribuït end-to-end: 16/16 PASS
  - worker A (subprocess, shard A) i worker B (subprocess, shard B): exit 0
  - mateix base_model_hash (A==B); shard manifests diferents
  - ETT_A=441 != ETT_B=434
  - FedAvg Coordinator -> adapter_1 (ett=875); oracle maxdiff=0.00e+00
  - round 2: adapter_pre_hash_A==B (adapter_1); adapter_2; oracle exacte
  - qualitat: val loss 2.8255 -> 2.1930; ppl 16.869 -> 8.962;
    score factual 2 -> 5 (no empitjora)
- recovery distribuït (R-Q1..R-Q5): 15/15 PASS
  - R-Q1: crash Worker A després d'APPLIED (subprocess PID 3521 mort) ->
    nou PID -> journal persistent -> recover sense optimizer -> completa;
    adapters finals IDÈNTICS (hash tolerant)
  - R-Q2 lease expiry REJECTED; R-Q3 reassignació; R-Q4 mid-training ->
    EXPIRED sense autograd; R-Q5 checkpoint retry idempotent
- regressió RC5.4: PASS (hashes dels fitxers congelats = tarball R3.1)
- manifest: PASS (304 fitxers, sha256sum -c 0 errors) + 6 artefactes
  (adapter_0/1/2.bundle, distributed_metrics.json, quality_before/after.json)

MANIFEST
--------
<coberts> fitxers regulars (incloent qwen3_pilot_output/, qwen3_pilot_logs/,
qwen_distributed_output/ i qwen_distributed_logs/) = <manifest> línies
sha256sum -c: 0 errors.

ENTREGA
-------
- meshtrainer_qwen_distributed_pilot_final_<commit>.tar.gz
- meshtrainer_qwen_distributed_pilot_final_<commit>.tar.gz.sha256
  (AUTORITATIU)
- DELIVERY_INFO.txt (aquesta versió, amb SHA real)
- verify.txt

NO IMPLEMENTAT (NOT AUTHORIZED): UI final, comptes d'usuari, cloud
orchestration, models grans, quantització complexa, producció, marketplace,
entrenament massiu.
