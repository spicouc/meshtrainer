"""MeshTrainer PRODUCT/APP — capa d'aplicació additiva sobre el core certificat.

Fase 1: Application Layer + Persistence + Dataset + Job lifecycle + API/CLI.
Core certificat b146723: INALTERAT. Aquest paquet NO importa cap driver de
test (generic_distributed_run.py, generic_recovery_tests.py) — només el core
(training_http_server, model_worker, model_round_coordinator, backends).
"""

__version__ = "0.1.0"
